import { defineConfig } from 'vite'

// You don't need to import Tailwind here manually when using PostCSS.
// Tailwind runs via postcss.config.js.

export default defineConfig({
  // No plugins needed for basic Tailwind use
})
